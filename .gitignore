# Node modules
node_modules/
frontend/node_modules/
backend2/api-gateway/node_modules/
backend2/auth-service/node_modules/
backend2/main-service/node_modules/

# It's generally recommended to commit package-lock.json or yarn.lock
# Ensure these are NOT ignored if you want reliable CI caching and builds.
# !**/package-lock.json # This would explicitly un-ignore, but usually not needed if not previously ignored.

# Environment files
.env
frontend/.env
frontend/.env.*
!frontend/.env.example # Example: explicitly track .env.example files
backend2/api-gateway/.env
backend2/auth-service/.env
backend2/main-service/.env
backend2/auth-service/.env.test

# Build artifacts
frontend/dist/
# Add any other service-specific build output if not within dist

# IDE & OS specific
.vscode/
.idea/
*.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Test coverage
coverage/
frontend/coverage/
backend2/api-gateway/coverage/
backend2/auth-service/coverage/
backend2/main-service/coverage/